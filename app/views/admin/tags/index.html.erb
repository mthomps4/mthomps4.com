<section class="my-4">
  <div class="flex justify-end w-full">
    <%= form_with model: @tag, url: admin_tags_path, method: :post, local: true, data: {turbo: false, turbo_stream: false}, class: "flex" do |f| %>
      <%= f.text_field :name, class: "w-full bg-white" %>
      <%= f.submit "Create", class: "dark:text-white px-4 py-2 bg-brand-blue-500" %>
    <% end %>
  </div>
  <h1 class="text-4xl text-brand-blue-700 dark:text-brand-blue-50 font-light text-center">Admin::Tags</h1>
  <div class="mt-8 flow-root">
    <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <% @tags.each do |c| %>
          <div class="flex items-center">
            <p class="mr-2"><%= c.name %></p>
            <%= link_to 'Edit', edit_admin_tag_path(c), class: 'text-brand-blue-500 dark:text-brand-blue-100 hover:text-brand-blue-300 hover:underline' %>
          </div>
        <%end%>
      </div>
    </div>
  </div>
</section>
<section>
  <%= paginate @tags %>
</section>
